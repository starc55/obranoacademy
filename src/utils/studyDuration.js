import {differenceInDays,differenceInMonths,differenceInYears} from 'date-fns';export function studyDuration(joinedDate){if(!joinedDate)return'Boshlanish sanasi yo‘q';const start=new Date(`${String(joinedDate).slice(0,10)}T00:00:00`),now=new Date();if(Number.isNaN(start.getTime())||start>now)return'Yangi qo‘shilgan';const years=differenceInYears(now,start);if(years>0){const months=differenceInMonths(now,start)-years*12;return months?`${years} yil ${months} oy`:`${years} yil`}const months=differenceInMonths(now,start);if(months>0){const cursor=new Date(start);cursor.setMonth(cursor.getMonth()+months);const days=differenceInDays(now,cursor);return days?`${months} oy ${days} kun`:`${months} oy`}const days=differenceInDays(now,start);return days>0?`${days} kun`:'Bugun qo‘shilgan'}
