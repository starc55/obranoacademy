import {forwardRef,useEffect,useRef,useState} from 'react';import {Check,Clock3} from 'lucide-react';
const times=Array.from({length:48},(_,i)=>`${String(Math.floor(i/2)).padStart(2,'0')}:${i%2?'30':'00'}`);
export const TimePicker=forwardRef(function TimePicker({name,value,defaultValue='',onChange,onValueChange,required,disabled,placeholder='Vaqtni tanlang',className=''},ref){const root=useRef(null),[open,setOpen]=useState(false),[internal,setInternal]=useState(defaultValue),current=value!==undefined?value:internal;useEffect(()=>{const close=e=>{if(!root.current?.contains(e.target))setOpen(false)};document.addEventListener('pointerdown',close);return()=>document.removeEventListener('pointerdown',close)},[]);const choose=time=>{if(value===undefined)setInternal(time);onValueChange?.(time);onChange?.({target:{name,value:time}});setOpen(false)};return <div ref={root} className={`time-picker ${open?'time-picker--open':''} ${className}`}><input ref={ref} type="hidden" name={name} value={current} required={required}/><button type="button" className="time-picker__trigger" disabled={disabled} onClick={()=>setOpen(x=>!x)}><span className={!current?'placeholder':''}>{current||placeholder}</span><Clock3/></button>{open&&<div className="time-picker__popover" role="listbox"><header><Clock3/><strong>Dars vaqtini tanlang</strong></header><div className="time-picker__grid">{times.map(time=><button type="button" role="option" aria-selected={current===time} className={current===time?'is-selected':''} key={time} onClick={()=>choose(time)}><span>{time}</span>{current===time&&<Check/>}</button>)}</div></div>}</div>})
